# examples

Example output feeds are **intentionally not included** in this repository.

The original development feed depicted a specific persona built from consented
reference photos, so it isn't appropriate to publish here. To see the framework in
action, run it with your own identity pack (see [`../pack_identidad/README.md`](../pack_identidad/README.md))
— a 10-scene batch costs roughly $0.30.
